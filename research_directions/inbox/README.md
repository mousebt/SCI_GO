# Research direction inbox

Drop raw direction notes here only when they are not yet ready for a formal direction workspace.

Once a direction is worth exploring, create an active workspace with:

```powershell
powershell -ExecutionPolicy Bypass -File scripts/new-research-direction.ps1 -Title "direction title"
```

